package edu.fiuba.algo3.modelo;

public class Tablero {

    private final personaje = Personaje new;
    private final pizarra = Pizarra new; "Segregar interfaz?"
    private final BloquesDisponibles = ListaDeBloques new; "Modelar una entidad ListaDeBloques, que permita agregar los blosques a una lista"
    private final algoritmo = Algoritmo new ; "Modelar una entidad Algoritmo que contenga una ListaDeBloques y nos permita trabajar con ellos"

    "Debemos pensar como solucionar la creacion de bloques, si los crea el tablero o si los crea el algoritmo, tal vez haya que usar algun patron creacional ?"

    public void agregarBloqueAlAlgoritmo(Bloque unBloque) {
        algoritmo.agregarBloque(unBloque) ;
    }

    public void ejecutarAlgoritmo(Personaje personaje){
        algoritmo.ejecutar(personaje) ;
    }

}